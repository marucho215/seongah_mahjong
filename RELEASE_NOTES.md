# 릴리즈 노트

## 1.1.0

1.0.0의 엔진과 CharacterAI는 그대로 두고, 브라우저 GUI에서 상대를 고르고 한 게임을 끝까지 둔 뒤 결과를 보고 다시 시작하는
흐름을 완성한 릴리즈다. 엔진 규칙, RNG, 점수 계산, 국 진행, CharacterAI 판단은 바뀌지 않았고, CustomAI가 없는 AI/사람 대국의
리플레이 바이트도 1.0.0과 같다(`tests/replayParity.test.ts`).

### 대국 준비 (로비)

- 로비에서 대국 방식(산마 3인 / 4마 4인)을 먼저 고르고, 같은 화면에서 좌석별 상대, CustomAI, 시드, 리플레이 저장을 정한다.
  대국 방식을 고르는 것만으로는 대국이 시작되지 않으며, "대국 방식 바꾸기"로 언제든 다시 고를 수 있다.
- 캐릭터 목록: 22명 전원을 확정된 한 줄 설명과 짧은 성향 태그로 보여준다. 내부 AI 수치는 화면에 보내지 않는다.
  초상화는 선택 필드 구조만 있고 이미지 영역은 없다.
- 같은 캐릭터는 한 좌석에만 앉을 수 있다. 무작위로 채우기 지원.

### 대국 중

- AI 진행 속도: 느림 / 보통 / 빠름 / 즉시 (이미 결정된 AI 턴을 보여주는 간격만 바꾼다).
- 자동 플레이 옵션(기본 꺼짐): 자동 쯔모기리, 울기 자동 패스(치·퐁·대명깡), 자동 화료(론·쯔모). 자동으로 한 선택도 사람 결정으로
  기록되어 리플레이에 남는다.
- 현재 샹텐과, 리치 여부와 관계없는 텐파이 대기패(공개 정보 기준 미확인 장수, 후리텐 표시 포함).
- 대국 그만두기: 확인 후 그 모드의 설정 화면으로 돌아간다. 그만둔 대국은 리플레이로 저장하지 않는다.

### 결과

- 국 결과: 화료자/친/방총자, 화료 손패(화료패·멘츠·북), 도라·우라도라 표시패, 역 목록(도라/아카도라/우라도라/북도라 분리),
  판·부·등급(만관~삼배만/역만, 엔진 기본점 기준)·최종 타점, 본장·공탁, 점수 이동. 1초 이내로 차례로 나타나며 누르면 바로 전부 보인다.
  더블 론은 화료마다 구분해 보여준다.
- 최종 결과: 엔진이 계산한 순위·점수·pt, 종료 사유, 실제 사용한 시드. "같은 설정으로 다시"(새 시드), "같은 시드로 다시",
  "설정 바꾸기"(마지막 모드의 설정 화면). 서버를 다시 켜지 않고 계속 둘 수 있다.

### 리플레이 뷰어

- `/replay.html` (로비에서 링크): 저장된 리플레이를 현재 엔진으로 다시 진행하며(시드/규칙/좌석/사람 결정 사용) 이벤트를 원본과
  순서대로 비교한다. 한 곳이라도 다르면 "현재 엔진 버전으로 정확히 재현할 수 없는 리플레이"로만 표시한다.
- 국/수순 이동, 화료·유국 지점 이동, 슬라이더/자동 재생/방향키, 모든 좌석의 손패·멘츠·북·강, 그 수 직전에 기록된 AI 판단.
- 리플레이 원본 파일은 읽기만 한다.

### CustomAI (1차)

- 로비에서 만들기/편집/복제/삭제, 상대로 앉히기. CharacterAI 공통 파라미터 15개를 한국어 이름과 설명이 붙은 0~100 슬라이더로
  편집한다(안전 범위는 CharacterAI 코드 기준, 자동 보정 없음). 판단 로직은 기존 CharacterAI 그대로다.
- `custom-ai/` 폴더에 AI 하나당 JSON 하나(내부 id 파일명, 형식 버전, 저장·불러오기 모두 같은 검증).
- CustomAI가 참가한 리플레이에는 그 좌석에만 실제로 쓴 프로필 스냅샷(`meta.seats[].customProfile`)을 저장하므로, 이후 CustomAI를
  고치거나 지워도 지난 리플레이는 같게 재현된다.
- 캐릭터 전용 특수 메커니즘과 가져오기/내보내기는 포함하지 않는다.

### 수정

- 퐁·치 직후 타패에서 손패의 마지막 패를 쯔모패처럼 떼어 그리던 표시 오류 (엔진이 쯔모 뒤 타패 요청에만 주는 `drawnTileId` 기준).
- 국/게임 종료 화면에서 새로고침하면 뒤의 작탁이 비어 보이던 문제 (마지막 장면으로 다시 그림).
- 시작 화면 서버 테스트의 keep-alive 경합으로 인한 간헐적 ECONNRESET (테스트 서버 설정).

### 개발

- `npm run test:e2e`: Playwright GUI 스모크 테스트 (로비 → 설정 → 대국 → 그만두기 / 종료 → 설정 바꾸기). `npm test`와 분리.
  처음 한 번 `npx playwright install chromium`이 필요하다.
- 새 테스트: 로비/허브, 대국 그만두기, 샹텐·대기, 쯔모패 id, 더블 론 결과, 리플레이 재현/바이트 동일성, 리플레이 뷰어 서버,
  CustomAI 스키마/저장소/서버.

### 호환성

- 리플레이 schema version은 2 그대로다. 추가된 것은 선택 필드(`meta.seats[].customProfile`)뿐이다.
- `npm run play:gui`는 이제 로비부터 열린다. 명령줄의 시드와 `--save-replays`는 설정 화면 초기값이 되고, 모드는 항상 로비에서
  고른다(`--mode`는 받기만 한다).
- 1.0.0 이전 엔진으로 만든 옛 리플레이는 뷰어에서 대개 재현 불가로 표시된다.

### 알려진 제한 (1.2 후보)

- 리플레이 뷰어에서 긴 리플레이를 열면 재현하는 동안 같은 GUI 서버가 몇 초 멈춘다.
- 새로고침 때 재생 중이던 AI 턴 장면은 다시 재생하지 않는다.
- 그 밖의 후보는 `HANDOFF.md` §10.
